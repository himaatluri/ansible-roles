---
- name: "[~ Build - Tools Server(s) ~]"
  hosts: all
  become: yes
  roles:
    - docker_ce
    - portainer
    - cleanup_ad_guard
    - pihole
    - grafana
    - node_exporter
    - prometheus_container
  vars:
    prometheus_version: "v3.1.0"
    node_exporter_version: "1.8.2"
...
